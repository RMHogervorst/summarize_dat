# summarize_components ####
#
#
#' give_general_summary Describes size, rows, columns, groupings, rownames, variablenames
#'
#' Collects information about the data.frame and stores them.
#'
#' @param dat Name of data.frame
#' @export
# give_general_summary(dat= dat) {
#         size<-object.size(dat)
#         rownames<-row.names(dat)
#         varnames<-names(dat)
#         if(is.grouped_df(dat)==TRUE){grouplabels<-attr(dat, "labels")}
#
# }  #seems easier to just put in rmarkdownfile
